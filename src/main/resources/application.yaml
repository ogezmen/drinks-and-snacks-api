ktor:
    application:
        modules:
            - de.okan.drink_and_snack_api.ApplicationKt.module
    deployment:
        port: ${PORT}

db:
    url: ${DB_URL}
    driver: ${DB_DRIVER}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

auth:
    jwt:
        secret: ${JWT_SECRET}
        issuer: "drink-and-snack-api"
        audience: "drink-and-snack-api-users"
        realm: "Access to the '/api' path"
        duration_in_ms: 60000