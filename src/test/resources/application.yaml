ktor:
    deployment:
        port: 8080

db:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;
    driver: org.h2.Driver
    username: ""
    password: ""

auth:
    jwt:
        secret: "your-256-bit-secret"